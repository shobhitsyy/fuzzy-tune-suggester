
// This component is obsolete from your requirements (big card expands directly), so remove all lyric UI
import React from "react";
import { Song } from "@/utils/fuzzyLogic";
const SongDetail = () => null;
export default SongDetail;
